Fix fib() off-by-one, canonicalize factor order, add retry-case test

fib(m) was indexing one off from the kernel's own stated convention
(F_1=1, F_2=1); corrected so 7/15/19 strands land on F_6=8, F_14=377,
F_18=2584 as documented. factors_from_period now returns the smaller
factor first rather than whichever gcd came out first. Added the two
Carrier16 labels that were missing (T, F). Split the old shor_n21_a5 test
into a correctly-named a=2 case and a new retry-case test covering the
standard Shor a^(r/2) ≡ -1 mod N situation, where no factor is found and
the result says so rather than guessing. menu.rs's shor entry updated to
describe the dialetheic subcommand this enables. Already covered by the
full test run: all 244 tests pass with these changes in place.
