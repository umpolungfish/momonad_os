I gave vox run in mOMonadOS the same real syscall coverage I just built into the standalone Vox binary: real read/write/open/openat/close through a host filesystem bridge, real mmap/brk over a bump-allocated heap, and a real argv/envp/auxv stack so the file's own entry point runs as an actual process instead of being called like one function with a fake return address.

vox run <file> now runs the whole file as a process; vox run <sym> <file> keeps calling one named function directly, unchanged. The help text and menu entry say plainly what still doesn't work: dynamic linking and a glibc-static binary's own thread-local-storage setup are further rungs, not built here — they halt or loop rather than pretending to succeed.

Verified inside mOMonadOS's own REPL against native execution, byte for byte, on the same four hand-written static ELF fixtures used to verify the standalone Vox binary: a write()+exit(), a brk()+mmap()+write() heap test, an openat()/read()/write()/close() round trip on a real file, and the original exit()-only entry point. Full suite still at 262 passed, 0 failed.
