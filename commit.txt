Serve the word instruments on vox lift's shortest open arm too

Same principle as imasm write/derive: a word came out of vox lift (the
shortest B-verdict function in a lifted binary) and the reader was left to
copy it into weight/banked/cycle/insert by hand. Ran the four automatically
on that one representative word instead of on every function in the file,
which would be one full report per function and mostly redundant.
