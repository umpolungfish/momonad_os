I documented the real syscall coverage in the README: what runs for real now (a statically-linked binary on direct syscalls, end to end), and what still doesn't (dynamic linking, and a glibc-static binary's own thread-local-storage setup), named plainly rather than folded into a vaguer "not yet" line.
