I added a Quickstart to the README: real commands a new user can run right after building, each one an actual computation rather than a status printout. Fibonacci anyon braid algebra, a real gate-to-braid compile, a braid drawn in the terminal, its Jones polynomial, Shor's period-finding actually factoring 15, the SIC-POVM existence proof status, and a real compiled ELF run through vox run with genuine syscalls.

Verified every one of these against the built hosted binary before writing it down, rather than trusting the menu text.
